/**
 * SelectionOverlay — placeholder.
 *
 * Reserved for a future on-map popover/highlight that appears when a zone
 * is clicked, showing quick zone details directly over the grid. Right
 * now zone selection detail is handled by the pages that render
 * <CityView /> (via their own side panels), so this stays a no-op to
 * avoid changing current appearance or duplicating that UI.
 */
export default function SelectionOverlay({ selectedCoord }) {
  return null;
}
