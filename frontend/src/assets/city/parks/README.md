# parks/

Reserved for future park asset(s) — distinct from `trees/`, this is for
park-specific structures (paths, benches, water features) rather than
vegetation scatter.

Referenced via `category: "parks"` entries in `../manifest.js`.
