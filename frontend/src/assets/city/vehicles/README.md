# vehicles/

Reserved for future vehicle assets, for whenever commuter/traffic
visualization renders actual moving vehicles rather than the current
aggregate traffic overlay placeholder (see
`src/renderers/grid2d/TrafficOverlay.jsx` and `src/lib/traffic.js`).
