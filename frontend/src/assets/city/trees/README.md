# trees/

Reserved for future decorative vegetation assets. Not currently referenced
by any zone type in `../manifest.js` — trees would likely be scattered
within park/residential zones rather than representing a zone type
themselves, so they'll probably be referenced differently (e.g. a density/
scatter config) once a 3D renderer needs them.
