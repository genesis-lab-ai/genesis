# buildings/

Reserved for future building assets (e.g. `.glb`/`.gltf` models) representing
residential, commercial, industrial, and mixed-use zones in a future 3D
renderer.

Referenced via `category: "buildings"` entries in `../manifest.js`. No
production assets exist yet — this directory exists so the eventual
structure is already in place.
